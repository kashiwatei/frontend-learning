let a = 0;
let b = 10;
let c = 20;

// 賦值運算
// a += 10; // a = a + 10;
// b += 10; // b = b + 10;
// c += 10; // c = c + 10;

// a -= 10; // a = a - 10;
// b -= 10; // b = b - 10;
// c -= 20; // c = c - 20;

//遞增（減）運算
// a++; // a = 1;
// b++; // b = 10 + 1;
// c++; // c = 20 + 1;

// a--; // a = a - 1;
// b--; // b = b - 1;
// c--; // c = c - 1;

console.log(a+b+c);
